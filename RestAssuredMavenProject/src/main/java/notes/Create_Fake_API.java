package notes;
/*
 *  Install jSon-server
 *  Run JSon-server
 * Create dummy API data
 * Invoke JSon-server URL on browser and check the API
 * 
	Step 1 : download NodJs
	Step 2 : Install JSon Server 
			> JSon server is a program that we are going to use for creating a local 
						API server and then a local API 
			> "npm install -g json-server" type this command in CMD it is for download jSon server
			> 'json-server --watch db.json' type this command also
					-- this command will create a db.json file on local host C:\windows\system32 
					
				now you can visit on http://localhost:300 there is an fake API 
	
	
	
*/